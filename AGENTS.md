# BackupRestore 开发验证约束

- 每次修改测试虚拟机启动项（BCD、默认项、一次性启动序列、WinRE 注册或 PE 部署）前，必须先创建并核验新的 Parallels 快照，记录快照 ID、目的和变更范围。快照失败时不得继续。
- 应用执行的一次完整启动变更事务也必须在触发前创建快照；进入 PE 后再手动变更或返回时，需要另建快照。不要复用早先快照来覆盖新操作。
- 用户恢复的正常 Windows 快照是回退基线；清理仅限确认属于本项目的旧包、测试任务和开发残留，不删除用户文件、系统恢复镜像、开发工具链或现有快照。
- 测试证据保存在已忽略的 `.test-artifacts/`；先验证本地测试及 Windows ARM64 构建，再部署并核对哈希。启动成功不等于备份还原成功。
